package com.example.swmandroid.ui.fullservice

import android.os.Bundle
import com.example.swmandroid.base.BaseActivity
import com.example.swmandroid.databinding.ActivityFullServiceBinding

class FullServiceActivity : BaseActivity<ActivityFullServiceBinding>({ActivityFullServiceBinding.inflate(it)}) {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}